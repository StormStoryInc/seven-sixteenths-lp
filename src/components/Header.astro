---
---

<header>
  <nav class="container">
    <a href="/" class="logo" aria-label="7/16 Home">7/16</a>
    <div class="nav-links">
      <a href="/terms">利用規約</a>
      <a href="/privacy">プライバシー</a>
    </div>
  </nav>
</header>

<style>
  header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    padding: 0;
    background: rgba(0, 0, 0, 0.8);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.06);
  }

  nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 64px;
  }

  .logo {
    font-size: 1.5rem;
    font-weight: 900;
    letter-spacing: -1px;
    color: var(--white);
    transition: opacity 0.2s;
  }

  .logo:hover {
    opacity: 0.7;
  }

  .nav-links {
    display: flex;
    gap: 2rem;
  }

  .nav-links a {
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--gray-400);
    transition: color 0.2s;
  }

  .nav-links a:hover {
    color: var(--white);
  }
</style>
